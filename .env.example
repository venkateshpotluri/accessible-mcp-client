# Accessible MCP Client Environment Configuration
# Copy this file to .env and update the values for your local development setup

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Anthropic API Key (Required for chat functionality)
# Get your API key from: https://console.anthropic.com/
# Format: sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Flask Secret Key (Required for session management)
# IMPORTANT: Generate a secure random key for production use!
# Use: python -c "import secrets; print(secrets.token_hex(32))"
# In development, you can use FLASK_SECRET_KEY or SECRET_KEY
FLASK_SECRET_KEY=your-secret-key-change-in-production

# =============================================================================
# FLASK CONFIGURATION (Recommended Pattern)
# =============================================================================

# Flask Environment (affects configuration and security settings)
# Options: development, production, testing
FLASK_ENV=development

# Flask-prefixed variables are automatically loaded by the application
# These follow Flask's recommended configuration pattern

# Application Settings
FLASK_MAX_MESSAGE_LENGTH=10000
FLASK_MAX_SESSION_TITLE_LENGTH=200

# Claude AI Configuration
FLASK_CLAUDE_MODEL=claude-3-5-sonnet-20241022
FLASK_CLAUDE_MAX_TOKENS=4000

# MCP Configuration
FLASK_MCP_TIMEOUT=30
FLASK_MAX_SERVERS=10

# Server Configuration
FLASK_PORT=5000

# =============================================================================
# LEGACY ENVIRONMENT VARIABLES (Backward Compatibility)
# =============================================================================

# These variables are still supported for backward compatibility
# but using FLASK_ prefixed versions above is recommended

# Flask Secret Key (alternative to FLASK_SECRET_KEY)
SECRET_KEY=your-secret-key-change-in-production

# Message and Session Limits
MAX_MESSAGE_LENGTH=10000
MAX_SESSION_TITLE_LENGTH=200

# Claude AI Configuration (legacy)
CLAUDE_MODEL=claude-3-5-sonnet-20241022
CLAUDE_MAX_TOKENS=4000

# MCP Configuration (legacy)
MCP_TIMEOUT=30
MAX_SERVERS=10

# Server Configuration (legacy)
PORT=5000

# =============================================================================
# PRODUCTION CONFIGURATION (Optional)
# =============================================================================

# These are typically set by your hosting platform in production
# Only needed if deploying to a custom environment

# Database URL (if using a database in the future)
# DATABASE_URL=postgresql://username:password@hostname:port/database

# Redis URL (if using Redis for sessions in the future)
# REDIS_URL=redis://localhost:6379

# Additional security headers (production)
# SECURE_SSL_REDIRECT=true
# SESSION_COOKIE_SECURE=true
# SESSION_COOKIE_HTTPONLY=true